select
	t1.STATE_ID as stateId,
	t2.LANGUAGE_CODE as languageCode,
	t2.DISPLAY_VALUE as displayValue,
	t2.DESCRIPTION as description
from
	RECHARGE_CARD_STATE_REF t1
	inner join RECHARGE_CARD_STATE_VALUES t2 on t1.STATE_ID = t2.STATE_ID
where
	t2.LANGUAGE_CODE = {2}

select
	t1.RELEASE_ID as releaseId,
	t1.RELEASE_MODULE as releaseModule,
	t1.RELEASE_TYPE as releaseType,
	t1.RELEASE_USER as releaseUser,
	t1.RELEASE_DATE as releaseDate,
	t1.RELEASE_MACHINE as releaseMachine,
	t1.RELEASE_VIEW as releaseView,
	t1.RELEASE_INFO as releaseInfo
from
	REG_RELEASE t1

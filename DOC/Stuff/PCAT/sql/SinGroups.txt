select
	t3.DISPLAY_VALUE as sinGroupRef,
	t1.GROUP_ID as groupId,
	t1.SERVER_ID as serverId,
	t1.SQL_QUERY as sqlQuery
from
	SIN_GROUPS t1
	inner join SIN_GROUP_REF t2 on t1.GROUP_ID = t2.GROUP_ID
	inner join SIN_GROUP_VALUES t3 on t2.GROUP_ID = t3.GROUP_ID
		and t3.LANGUAGE_CODE = {2}

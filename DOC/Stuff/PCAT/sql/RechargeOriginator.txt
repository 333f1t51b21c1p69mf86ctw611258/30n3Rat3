select
	t1.ORIGIN_ID as originId,
	t2.LANGUAGE_CODE as languageCode,
	t2.DISPLAY_VALUE as displayValue,
	t2.DESCRIPTION as description
from
	RECHARGE_ORIGINATOR_REF t1
	inner join RECHARGE_ORIGINATOR_VALUES t2 on t1.ORIGIN_ID = t2.ORIGIN_ID
where
	t2.LANGUAGE_CODE = {2}

select
	t1.MARKUP_ID as markupId,
	t2.RESELLER_VERSION_ID as resellerVersionId,
	t2.MARKUP_RATE as markupRate,
	t3.LANGUAGE_CODE as languageCode,
	t3.DISPLAY_VALUE as displayValue,
	t3.DESCRIPTION as description
from
	MARKUP_RATING_KEY t1
	inner join MARKUP_RATING_REF t2 on t1.MARKUP_ID = t2.MARKUP_ID
	inner join MARKUP_RATING_VALUES t3 on t2.MARKUP_ID = t3.MARKUP_ID
		and t2.RESELLER_VERSION_ID = t3.RESELLER_VERSION_ID
where
	t2.RESELLER_VERSION_ID = {0}
	and t3.LANGUAGE_CODE = {2}

select
	t1.AUT_GROUP_MAP_ID as autGroupMapId,
	t1.RESELLER_VERSION_ID as resellerVersionId,
	t1.APPLICATION_ID as applicationId,
	t2.SERVICE_VERSION_ID as serviceVersionId,
	t2.IS_DEFAULT as isDefault,
	t2.IS_INTERNAL as isInternal,
	t2.APPLICATION_CODE as applicationCode,
	t3.LANGUAGE_CODE as languageCode,
	t3.DISPLAY_VALUE as name,
	t3.DESCRIPTION as description
from
	AUT_GROUP_MAP t1
	inner join APPLICATION_ID_REF t2 on t1.APPLICATION_ID = t2.APPLICATION_ID
	inner join APPLICATION_ID_VALUES t3 on t2.APPLICATION_ID = t3.APPLICATION_ID
		and t2.SERVICE_VERSION_ID = t3.SERVICE_VERSION_ID
where
	t1.RESELLER_VERSION_ID = {0}
	and t2.SERVICE_VERSION_ID = {1}
	and t3.LANGUAGE_CODE = {2}
